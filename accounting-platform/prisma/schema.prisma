generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Run `docker compose run --rm app npx prisma db pull` after first `docker compose up`
// to populate this file with all 28 models from the loaded schema.
